#include "main.h"
#include <stdarg.h>

/**
 *_prints - prints a character
 *@arg:no of arguement
 *@va_list:pointer to arguement
 * Return:count
 */
int _prints(arg, va_list)
{
	char *str;
	int count, count = 0;
	int i, i = 0;


	str = var_arg(list, char *str)
	while (str[i])
	{
		putchar (str[i];
		count++;
		i++;
	}
	return (count);
